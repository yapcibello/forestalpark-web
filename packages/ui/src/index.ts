// @fp/ui — componentes compartidos de forestalpark-web.
//
// ESQUELETO fase `estructura`. Los componentes reales (Header, Footer,
// CookieConsent, etc.) se implementan en la fase 7 (UI). Por ahora el
// paquete solo expone su punto de entrada para que el monorepo resuelva
// el import `@fp/ui` sin errores de build.

export {};
